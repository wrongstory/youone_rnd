name: CI

on:
  pull_request:
  push:
    branches: [dev, main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_test
      M03_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m03_test
      M04_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m04_test
      M05_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m05_test
      M06_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m06_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Create isolated feature test databases
        run: |
          psql "$TEST_DATABASE_URL" --set ON_ERROR_STOP=1 --command "create database youone_m03_test"
          psql "$TEST_DATABASE_URL" --set ON_ERROR_STOP=1 --command "create database youone_m04_test"
          psql "$TEST_DATABASE_URL" --set ON_ERROR_STOP=1 --command "create database youone_m05_test"
          psql "$TEST_DATABASE_URL" --set ON_ERROR_STOP=1 --command "create database youone_m06_test"

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify workspace
        run: pnpm check

  r01-request-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_r01_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_r01_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      R01_ADMIN_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_r01_test
      R01_REQUEST_DATABASE_URL: postgresql://youone_request_login:request-test@localhost:5432/youone_r01_test
      PSQL_BIN: psql
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify request PostgreSQL runtime
        run: pnpm exec vitest run tests/release-gate/r01-postgres.integration.test.ts

  r02-request-auth-session:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_r02_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_r02_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      R02_ADMIN_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_r02_test
      R02_IDENTITY_RESOLVER_DATABASE_URL: postgresql://youone_identity_resolver_login:resolver-test@localhost:5432/youone_r02_test
      PSQL_BIN: psql
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify Supabase active-session identity boundary
        run: pnpm exec vitest run tests/release-gate/r02-postgres.integration.test.ts

  m07-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m07_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m07_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M07_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m07_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M07 PostgreSQL boundary
        run: pnpm exec vitest run tests/m07/postgres.integration.test.ts

  m08-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m08_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m08_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M08_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m08_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M08 PostgreSQL boundary
        run: pnpm exec vitest run tests/m08/postgres.integration.test.ts

  m09-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m09_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m09_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M09_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m09_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M09 PostgreSQL boundary
        run: pnpm exec vitest run tests/m09/postgres.integration.test.ts

  m10-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m10_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m10_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M10_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m10_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M10 PostgreSQL boundary
        run: pnpm exec vitest run tests/m10/postgres.integration.test.ts

  m11-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m11_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m11_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M11_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m11_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M11 PostgreSQL boundary
        run: pnpm exec vitest run tests/m11/postgres.integration.test.ts

  m12-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m12_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m12_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M12_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m12_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M12 PostgreSQL boundary
        run: pnpm exec vitest run tests/m12/postgres.integration.test.ts

  m13-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m13_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m13_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M13_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m13_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M13 PostgreSQL boundary
        run: pnpm exec vitest run tests/m13/postgres.integration.test.ts

  m14-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m14_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m14_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M14_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m14_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M14 PostgreSQL boundary
        run: pnpm exec vitest run tests/m14/postgres.integration.test.ts

  m15-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m15_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m15_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M15_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m15_test
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M15 PostgreSQL boundary
        run: pnpm exec vitest run tests/m15/postgres.integration.test.ts

  m16-security-operations:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_m16_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_m16_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      M16_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m16_test
      PSQL_BIN: psql
      PG_DUMP_BIN: pg_dump
      PG_RESTORE_BIN: pg_restore
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Create isolated recovery database
        run: psql "$M16_TEST_DATABASE_URL" --set ON_ERROR_STOP=1 --command "create database youone_m16_recovery_test"

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify M16 security matrix and audit boundary
        run: pnpm exec vitest run tests/m16/postgres.integration.test.ts

      - name: Verify clean upgrade and full restore drill
        env:
          M16_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_m16_recovery_test
        run: pnpm exec vitest run tests/m16/integration-recovery-postgres.test.ts

  r03-offline-handlers:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: youone_r03_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d youone_r03_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      R03_TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/youone_r03_test
      PSQL_BIN: psql
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify R03 offline handler PostgreSQL boundary
        run: pnpm exec vitest run tests/release-gate/r03-postgres.integration.test.ts

  r04-private-storage-recovery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/setup@v1

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify R04 private Storage and recovery boundary
        run: pnpm exec vitest run tests/release-gate/r04-private-storage.test.ts tests/m05/storage-contract.test.ts tests/m16/integration-recovery-manifest.test.ts tests/architecture/layer-boundaries.test.ts
